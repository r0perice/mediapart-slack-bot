name: Build Docker Image

on:
  push:
     branches:
       - develop

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
   ## BUILD IMAGE
    - name: docker build
      run: docker build -t docker.pkg.github.com/r0perice/mediapart-slack-bot/mediapart-slack-bot:develop -f Dockerfile .